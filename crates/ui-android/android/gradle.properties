# AndroidX is required by the dependencies this app uses (androidx.leanback,
# androidx.media3, androidx.games); AGP refuses to build without this flag
# explicitly set.
android.useAndroidX=true

org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
